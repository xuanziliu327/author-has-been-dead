# 我以后要怎么改这个网站（不会前端版）

## 改网站里的文字

网页里的正文直接来自 `第一章` 文件夹中的 Markdown 文件。

- 想改协查通告：修改 `第一章/协查通告.md`
- 想改群众消息记录：修改 `第一章/群众消息语音记录.md`
- 想改现场、物品或电子设备记录：修改对应的 `.md`
- 想改四篇文章：修改 `第一章/疑似死者文件记录册/` 里的对应 `.md`
- 想补隐藏页文字：修改 `第一章/隐藏页文档.md`

保存后 commit、push，GitHub Pages 会自动重新部署。不要修改 `dist/`，它每次构建都会重新生成。

## 补上故事 MP3

把录好的文件准确命名为：

```text
story.mp3
```

放到：

```text
第一章/story.mp3
```

然后 commit、push。不需要修改任何代码。下次构建会自动把“音频素材待补充”替换为播放器。

## 补两个结局 MP4

文件必须准确命名为：

```text
ending_yes.mp4
ending_no.mp4
```

把它们放到：

```text
第一章/ending_yes.mp4
第一章/ending_no.mp4
```

然后 commit、push，不需要修改代码。网页会自动替换占位视频。

视频推荐使用 MP4 容器、H.264 视频、AAC 音频，这样浏览器兼容性最好。GitHub 普通文件不能超过 100MB，最好把单个文件控制在 50MB 以内。太大就先压缩，不要直接上传几百 MB。

## 替换现有音效、PDF 或图片

想换音效时，保持原文件名覆盖 `第一章/` 中的文件：

```text
river.mp3
button.mp3
gasp.mp3
```

PDF 和房间图也可以保持同名直接覆盖。如果真的改了文件名，需要同时修改 `src/config/game.ts` 的 `mediaFileNames`，以及 `scripts/sync-runtime-assets.mjs` 中的必需文件清单。

## 用 GitHub 网页上传

1. 打开 GitHub 仓库。
2. 进入 `第一章` 文件夹。
3. 点击 **Add file → Upload files**。
4. 把文件拖进去。
5. 点击 **Commit changes**。
6. 等待仓库的 **Actions** 运行完成。
7. 打开 Pages 网站并刷新。

## 用本地 Git 上传

```bash
git add .
git commit -m "update content"
git push
```

## 在自己电脑上预览

先安装 Node.js，然后在项目根目录运行：

```bash
npm install
npm run dev
```

打开终端显示的 localhost 地址。结束预览时，在终端按 `Ctrl + C`。

## 改特殊日期、五分钟间隔或 credits

打开 `src/config/game.ts`：

- `specialDate`：特殊日期
- `riverIntervalMs`：普通水声间隔，单位是毫秒；五分钟是 `300000`
- `creditsLines`：credits 文字
- `creditsDurationMs`：credits 总时长
- `noEndingReturnRoute`：“否”结局最后返回哪里

## 改密码或答案

不要自己计算 SHA-256。运行：

```bash
npm run hash -- "新的六位密码"
```

复制终端输出的 hash，粘贴到 `src/config/game.ts` 的 `passwordHash`。姓名和年龄也用同一个命令计算，分别更新 `identityNameHash` 或 `identityAgeHashes`。配置里只放 hash，不要把答案写进注释。

## 出问题时先做什么

1. 去 GitHub 的 **Actions** 页面看最近一次运行是不是红色。
2. 在本地运行 `npm run build`，看终端给出的具体错误。
3. 不要手工编辑 `dist/`。
4. 如果媒体没有出现，先检查文件名、扩展名和大小写是否完全一致。
5. 如果是必需的音效、PDF 或 PNG 被误删，构建会主动报错；把同名文件放回 `第一章/` 即可。
